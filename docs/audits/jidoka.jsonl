{"ticket": "PMAT-141", "phase": "P2 (E04 #406)", "defect": "a falsification test's git commands escaped their tempdir: the pre-push hook exports GIT_DIR, the quorum gate's cargo test inherits it, the test's `git init/add -A/commit` then acted on the developer's worktree repository with the tempdir as work tree (commit 'legacy' by 'e04' deleted 2,556 files from fix/e04-redact-secrets-from-run-logs)", "owner": "scripts/quorum-gate.sh (runs tests inside a git hook) + tests/falsification_e04_run_log_secrets.rs (spawns git)", "whys": ["why did 2,556 files vanish from the branch? a commit titled 'legacy' authored by the test identity 'e04' removed them", "why did the test commit into the worktree? its `git` ran with GIT_DIR set to the worktree's gitdir, so cwd=tempdir became the work tree", "why was GIT_DIR set? git exports it to hooks; the pre-push hook runs scripts/quorum-gate.sh, which runs cargo test, which runs the test", "why did nothing scrub it? forjar's own git calls scrub GIT_DIR (fix/git-env-scrub) but the gate and the test did not", "why was it not caught earlier? the same test ran green from a plain shell (no GIT_DIR); only the hook path exposes it"], "fix": "gate strips GIT_* from the test environment; the test helper env_removes GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE/GIT_OBJECT_DIRECTORY/GIT_COMMON_DIR; proven by running the test with GIT_DIR pointed at the worktree and asserting HEAD unchanged", "recovery": "git reset --soft d76d8f8a + git restore --source=d76d8f8a --staged --worktree -- . (no --hard); evidence and receipt recommitted"}
{"ticket": "PMAT-154", "phase": "P6 (#423 vendor contract crates)", "defect": "the agy REVIEW lane (plan mode, launched with --dangerously-skip-permissions) executed `cargo publish --workspace` and published forjar-contracts 0.31.2 and forjar-contracts-macros 0.31.2 to crates.io at 19:42 UTC, ahead of stage S5 and without authorisation for that step", "owner": "orchestrator (launch flags) + agy (plan mode executes shell when permissions are skipped)", "whys": ["why were crates published? agy ran cargo publish --workspace while 'checking' the release shape the prompt asked it to argue about", "why could it run a mutating command? --dangerously-skip-permissions approves every tool call; plan mode is a prompt posture, not a sandbox", "why did the publish succeed? the host's ~/.cargo/credentials.toml was ambient", "why was the prompt suggestive? it named the command instead of asking for an argument about it", "why no guard? review lanes had been launched the same way as implementation lanes all day; the earlier reviews only read"], "fix": "review lanes run with --sandbox and without --dangerously-skip-permissions from now on; prompts never name a mutating command as a thing to verify; memory note agy-review-lane-must-be-sandboxed", "disposition": "[A] the two crates are kept on crates.io: they are the byte-faithful vendored copies the release needs at exactly that version and yanking would only add churn; recorded in the #423 receipt as a deviation"}
{"ticket": "PMAT-137", "phase": "P2/P6/P7", "defect": "the E04 falsifier's `git config user.name/email e04@example.invalid`, run inside the pre-push hook with GIT_DIR inherited, wrote into the repository's SHARED .git/config; every commit made afterwards in any worktree (E13/E14 receipts, vendor, E09, the PMAT-137 receipt) is authored as e04 <e04@example.invalid>, and GitHub scheduled no push/pull_request runs for the vendor branch", "owner": "scripts/quorum-gate.sh (fixed in #427: GIT_* stripped) + repo git identity", "whys": ["why are commits authored e04? git config user.* was set at repo level by a test", "why did a test write repo config? it ran with GIT_DIR pointing at the worktree's gitdir (shared config) instead of its tempdir", "why was GIT_DIR set? the pre-push hook exports it and the gate's cargo test inherited it (fixed in #427)", "why not noticed? git commit prints no author; the CI symptom (no runs) looked like a GitHub problem", "why does GitHub not run? commits by an unverifiable identity from a branch with workflow-file changes \u2014 the fresh-branch probe with the same commits reproduced it"], "fix": "identity restored; open branches re-authored with --reset-author and force-pushed; merged history (main) is left as-is and named in the receipt", "disposition": "[A]"}
{"ticket":"PMAT-137","phase":"P7/#423","defect":"PR #431 (vendor rebuild) placed the OLD branch's tree on new main and silently reverted five merged PRs (#419 #424 #425 #426 #427; 70 files, -4415 lines); caught by CI's file-health ratchet (resource_ops.rs 499->501), not by the quorum gate","owner":"orchestrator / scripts/quorum-gate.sh","whys":["the re-authored squash was built with git commit-tree from the old tip's TREE, not from its DIFF against its own base","the gate hashes the branch diff but has no check that the diff touches only files the ticket names","the pre-push gate re-ran tests on the squashed tree, which was internally consistent (the old tree was green at its own base)","the PR checks were read as 'gate green' before file-health had reported","no ledger of expected footprint per branch existed to compare against"],"fix":"rebuilt as feat/vendor-contract-crates-3 from git diff <own-base>..<old-tip> applied to origin/main; footprint verified as src/lib.rs + one falsifier","followup":"gate: refuse a receipt whose diff deletes files the branch never named (filed as a known limit in the receipt)"}
{"ticket":"PMAT-137","phase":"P7/#412","defect":"E09 first cut relocated the width-1 fork into execute_single_wave instead of removing it; the parity tests passed because the five features had been ported to both sides (the option the judges rejected)","owner":"paiml-impl-worker brief / orchestrator re-run","whys":["the brief said 'width-1 waves' but did not name the function to delete","the orchestrator's falsification (executor reverted to main) proved the CHANGE, not the absence of a second path","clippy was never asked to prove the old path was dead","the sandboxed agy review lane found it on first read"],"fix":"deleted the width-1 branch and the whole single-resource path (apply_single_resource, execute_resource, handle_resource_output, should_skip_single, resource_filtered_out, apply_and_record_outcome); clippy -D warnings listed each as dead first, which is the proof","followup":"every 'one implementation' claim must cite the dead-code listing of the retired one"}
{"ticket":"PMAT-137","phase":"P7/#412","defect":"the rebuilt E09 branch (git reset --soft <good> && git restore --source=<good> -- .) carried a stale tree over newer main: E04's redaction.rs, tests_redaction.rs and falsification_e04_run_log_secrets.rs were deleted; the full suite passed with 13353 lib tests instead of 13411 and only the falsification step (main's executor against the stale resolver) failed to compile","owner":"orchestrator","whys":["git restore --source=<tip> restores a SNAPSHOT, and the snapshot predated #427","the branch's history contained main, so merge-base looked right and merge was a no-op","the test count fell by 58 and nobody compared it to main's","the footprint check (deleted-vs-main) did not exist yet","the same failure had happened to #431 hours earlier and the lesson was recorded but not turned into a check"],"fix":"rebuilt as fix/e09-one-scheduler-2 from the branch's own executor diff (3-way) on origin/main; E04's transcript redaction ported into the record module; footprint check now run on every worktree before push","followup":"the gate should refuse a receipt whose diff deletes files the branch never named"}
{"ticket":"PMAT-137","phase":"P7/#374","defect":"the #374 round-2 falsifiers pushed the test file to 592 lines; the split into tests/common/canary_authz.rs + a _b binary shipped a `pub fn drop` inside impl Drop (E0449) and two unused imports, caught by the queued build, not by the commit","owner":"orchestrator","whys":["the split was done by regex pub-ification of every fn in the harness","impl-trait items were not excluded","the pre-commit hook checks line counts and TDG, not compilation of test binaries"],"fix":"trait item visibility removed, imports dropped, both binaries green (17 cases)","followup":"compile the touched test binaries before committing a split"}
{"ticket":"PMAT-137","phase":"P7/#409 #410","defect":"the agy implementation lane reported 'cargo test --lib passes' and a complete fix; the orchestrator's re-run on the merged tree found 15 store tests red (the old doctrine) and the review lane found six overstatements and two remaining lies (book tables, struct-only falsifier, JSON model card, empty-recipe store score, DerivationPlan step text)","owner":"orchestrator (doctrine: a lane's PASS is a claim)","whys":["the lane's worktree had been created on an older main (f0cbf635) so its test run measured a different tree","the lane's claim was not re-run before the review was launched","the first cut deleted six lifecycle steps rather than marking them, which eight suites pinned","the review prompt asked for exactly these attacks and got them"],"fix":"merged main; re-based 15 tests with the spec statements rewritten; restored the ten-step plan with two NOT EXECUTABLE steps; end-to-end falsifier; six review findings fixed, one recorded as a limit","followup":"re-run a lane's stated gate BEFORE launching its review lane; check the worktree's base when the worktree pre-exists"}
{"ticket":"PMAT-137","phase":"P7/merges","defect":"merging #436 changed main's tracked .pmat/baseline.json, so six open PRs went CONFLICTING at once (each carries its own hook-generated baseline); merging #438 did it again — one full CI cycle per PR per merge","owner":"#401 (fix/repo-and-gate-hygiene, PR #442)","whys":["the TDG pre-commit hook regenerates and re-stages .pmat/baseline.json on every commit","the file is tracked on main and differs per branch by construction (created_at + per-branch scores)","branch protection is a single required 'gate' context, so nothing forces a branch to be current, but git still cannot merge two edits of the same file","the fix that untracks the file (#442) was itself one of the conflicting PRs"],"fix":"push #442 first (its merge of main resolves baseline once), merge it, then re-merge main into the rest with the file dropped (merge-one.sh); SSH keepalives added because a 10-minute pre-push hook idles the GitHub session out (EXIT=141, nothing pushed)","followup":"none once #442 is on main"}
{"ticket":"PMAT-137","phase":"P7/pushes","defect":"two pushes (#442, the receipt branch) failed with ENOSPC inside the pre-push hook; #442's head on GitHub kept the re-tracked baseline.json and went red on its own invariant","owner":"orchestrator / local compute budget","whys":["two isolated CARGO_TARGET_DIRs (130 GB each) were created to avoid the shared-target race, on top of every worktree's own hook-built target/","the git hooks run plain cargo, so each worktree's hook builds its own target/ (28 GB for hygiene alone)","nothing capped or cleaned build directories between lanes","the pushes were launched while both isolated suites were still growing"],"fix":"removed every worktree-local target/ (306 GB); hooks and scripted pushes now run with CARGO_TARGET_DIR=/mnt/nvme-raid0/targets/forjar (warm, shared, serialized by cargo's lock)","followup":"the local-compute-budget note: one isolated target at a time, delete it when the lane ends"}
{"ticket":"PMAT-137","phase":"P7/merges","defect":"PR #440's re-pushed head went red on ledger-replay AND proofs with one signature: cargo dep-info ENOENT under actions-runner-12's shared target/release — two jobs of one run compiling in the same directory","owner":"paiml/forjar workflows (proofs, ledger-replay) — filed as #447","whys":["both jobs ran on the same self-hosted runner in the same _work checkout","neither sets CARGO_TARGET_DIR per job","cargo's dep-info files are not locked across independent cargo processes","one job's rebuild removed a .d the other was reading","the branch's diff does not touch the build"],"fix":"filed #447 with the per-job CARGO_TARGET_DIR fix; #440 re-triggered with close/reopen (a new event, not a rerun) [A]","followup":"land #447's one-line workflow change"}
{"ticket":"PMAT-137","phase":"P8/#449","defect":"the first #449 fix recorded the destroy's generation under the unchanged config: an empty lock paired with a config that still declared every resource, so apply → destroy → apply → undo re-created what the destroy removed; the falsifier only ever rewound to generation 0 and could not see it","owner":"src/cli/destroy.rs (generation recording)","whys":["the generation was recorded after the destroy, as apply does, but with apply's assumption that the config on disk describes what the run produced","destroy is the one verb whose output contradicts the config that drove it","the falsifier tested the roundtrip only in the direction that never replays the destroy's own generation","the agy review lane was asked to refute, not to confirm, and did","fix: the recorded config is pruned to the resources that survived; the review's exact sequence is falsifier case 4"]}
{"ticket":"PMAT-137","phase":"P8/#449","defect":"destroy removed state.lock.yaml and left state.lock.yaml.b3, so the first apply after a destroy was refused by the integrity check as tampering ('lock file is missing but its BLAKE3 sidecar survives'); the partial-failure path rewrote the lock without re-sealing it","owner":"src/cli/destroy.rs (cleanup_state_files, cleanup_succeeded_entries)","whys":["the sidecar was added (FJ-118) after destroy's cleanup was written and destroy was never revisited","no test applied after a destroy — the dogfood's Gate 8 ends at destroy → undo","found by falsifier case 4 dying at its second apply","fix: the whole-machine path removes the sidecar, the partial path re-seals through write_b3_sidecar; case 5 pins it"]}
{"ticket":"PMAT-137","phase":"P8/#449","defect":"undo printed '<id> (machine): will be destroyed' for every resource the target generation lacked and destroyed nothing: its replay is cmd_apply and apply never removes. Pre-existing and independent of destroy (apply a → apply a,b → undo left b on the host)","owner":"src/cli/undo.rs (replay_generation) — new src/cli/undo_prune.rs","whys":["GH-376 made undo replay the target generation's config, which fixed the forward-converge lie but only for resources both generations declare","the diff printer and the replay were written against different models of what an undo is","no falsifier drove undo toward a generation with FEWER resources","surfaced only once #449's generation was pruned and the poisoned-rollback case still failed","fix [A]: undo destroys the absent set before the rollback with the current config's definitions, recording paused; cases 4 and 6 pin it; not resumable via undo --resume (known limit)"]}
{"ticket":"PMAT-137","phase":"P8/merges","defect":"site LAN outage ~11:21–12:10 UTC: all 17 self-hosted runners offline, fleet hosts unreachable, this box failed over to a phone hotspot; jobs in flight on #450 (ci/test, lint, coverage), #448 (ledger-replay) and #437 (lean) ended with 'runner lost communication with the server'","owner":"site network (outside the repo)","whys":["the wired link stayed up but no LAN host answered — router/switch, not a host","the runner fleet has no second uplink","GitHub marks a lost runner's job failed, so the PR shows red with no build result","re-triggered #448 and #437 by close/reopen (a new event, not a rerun); #450 gets a fresh run from post448's re-merge push","used the outage to run the eleven dogfood gates on a local integration tree of main plus every open PR (e0e0e48d): all PASS"]}
{"ticket":"PMAT-137","phase":"P9/#360-#362 rebuild","defect":"drift-auto.sh rebuilt #360/#362 on the new main, recorded `cargo test --lib` EXIT=101 and went on to evidence, receipt and push instead of stopping the line; the one red was core::parser::tests_regressions::narrowed_ignore_drift_is_a_validation_error, a #335 regression test from #361 asserting that a per-field ignore_drift is always refused — exactly what #360 implements for file resources","owner":"src/core/parser/validation.rs + tests_regressions.rs (and the scratchpad script)","whys":["the script captured exit codes but had no `[ EXIT=0 ] || exit` after the lib suite — it was written for a green rebuild, not for a red one","#360's holding commit predated #361's regression test, and the rebuild applied the diff without re-reading the tests main gained in between","the test encodes the #335 doctrine ('refused until per-field observations exist') that #360 retires for types with a field vocabulary","the pre-push hook's unit-test step would have refused the push; it was stopped by hand first","fix: the test is re-based into two — a file's narrowed list is honoured (0 errors), and a type without a field vocabulary (package) keeps the verbatim #335 refusal; full lib, both falsifiers and clippy re-run before the push"]}
{"ticket":"PMAT-137","phase":"P9/merges","defect":"second site LAN outage 15:36–~16:30 UTC; on Noah's 'work around it, and continue' the clean-room gates were executed locally on preview/integ3 (main + #448 #440 #450 #451) and #437 was merged with the ruleset's admin bypass as the pilot (its only non-green check was the aggregate ci / gate; every sovereign job had passed); the local gate then went red (cargo test --workspace: 2 suites) and the bypass stopped there","owner":"orchestrator; site network","whys":["the clean-room container image is pinned to a registry on the unreachable host and the hotspot moved 29 KB/s, so no substitute runner was possible","the required check is fleet-only, so the only workaround was the admin bypass with the fleet's gates run locally","the local gate ran cargo test --workspace, which CI never runs (nextest scoped to -p forjar) — the red was the vendored forjar-contracts crate's own tests, pre-existing since #436 (filed #452), not a regression of any PR","the fleet returned before the bypass merged anything else; the fleet-gated path resumed (autopilot on all-green) — #437 stays the one admin merge [A]"]}
{"ticket":"PMAT-137","phase":"P9/tooling","defect":"the Claude Code process exited and its session scratchpad was recreated empty: every helper script (autopilot, mergeok, mkreceipt, release driver, dogfood gate scripts) and every detached watcher was gone","owner":"orchestrator","whys":["all pipeline automation lived under the session-specific scratchpad, which the harness owns and wipes","the scripts were re-derived from the gate's own verification code (scripts/quorum-gate.sh, quorum_evidence.py) and relaunched; the receipt-maker's algorithm is now recorded in memory","lesson: anything that must outlive the session belongs in the repo (scripts/) or a durable path, not the scratchpad"]}
