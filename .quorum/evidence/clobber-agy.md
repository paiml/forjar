# Quorum evidence — PMAT-230 — agy

- Version: agy 1.2.0; lane guard installed; nine sandboxed lanes through the delegate's fixed calling form (`--sandbox --dangerously-skip-permissions --print-timeout 25m --output-format json --json-schema <quorum schema>`), writes=false, three per round.
- Conversations: conv-c4f6ed9a,conv-9ac5e9ef conv-cb48b6c9,conv-7bafa035 conv-01bfac91,conv-a18d4087 conv-2380ce3b,conv-b4eb714e conv-96287542.
- Round 1 on 469cc9a9 (0/3 PASS): the rule matched the literal string `gh release download`; the claim misquoted gh's error; the flag on binary-release.yml is not needed there. Round 2 on b02d26b4 (1/3 PASS): a trailing `# --clobber` comment and a `--pattern "*--clobber*"` both satisfied a substring check; a backslash between `gh` and `release` walked past the finder; an innocent `gh release upload --title download` was flagged. Round 3 on 5d1920f6 (0/3 PASS): `release` was read as any token after `gh`; two commands on one continued line lent each other flags; and — the finding that mattered most — `homebrew` clones into the fixed path `/tmp/tap`, so the fix would have moved the failure two steps rather than removed it.
- **No round passed and none is claimed to have passed.** What closed the ticket is not a green round: it is that every refutation that reproduced was fixed and the fix measured by a twelve-case mutation battery, and that the ones deliberately left alone are named in the digest with the reason.
- All three delegate dispatches hit the 30-turn cap after their lanes had written their files; the orchestrator read `<out_dir>/lane-*.json` and ran `lane-reduce.sh` itself each time, the same recovery as PMAT-222..227.
- Residue: `git status --porcelain` carried only the untracked files that predate this session before and after every round; no lane wrote to the repository and no lane copied a `target/`.
