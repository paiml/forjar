# Quorum evidence — PMAT-227 — pmat and the gates

- Roadmap entry PMAT-227 minted with `pmat work add … -t kind:triage,orch:fable`, then `kind: triage`, four acceptance criteria and `orch-basis:release` patched inside the one row textually; label `release:v1.29.0` applied at mint time with `scripts/release-goal.sh tag PMAT-227 v1.29.0` — the open window's tag, which is the continuous half of PMAT-225's brief.
- `kind-gate.sh`: `kind=triage ticket=PMAT-227 files=0`. The skill's own kind:triage rail (an issue-universe snapshot partitioned into batches) is built for GitHub-issue triage and does not fit a ledger booking; this receipt keeps the code-shaped rows and says so rather than declaring a shape it did not follow.
- `model-gate.sh` REFUSED this ticket while the session model was fable — `model-gate: refused: model fable not permitted for PMAT-227 (fable)`, because a `kind: triage` row admits opus only — and ADMITTED it after the operator switched the session model mid-ticket: `model=opus class=opus decision=admit basis=file`. Both runs are recorded; the work before the switch was the ledger booking itself, which the gate is not a judge of, and no lane was dispatched under the refusal.
- `target-guard.sh`: PASS. `goal.sh set`: refused under R-5 (one ticket per session, naming PMAT-223) — the fifth ticket of this session, proceeding on the user's verbatim instruction quoted in PMAT-226's roadmap row; `goal.sh worker` declared the review lane (ph2.quorum, width 3).
- Routing (`route.sh`, copied verbatim): review `route=agy-quorum w=1.00 basis=absent effort=1[U]` (followed, three lanes); orchestration `route=self w=100.00 basis=absent` (the booking, the receipts, the push).
- Gates: `scripts/dogfood/tagged.sh` over main's HEAD with this ledger — `GATE T PASS 6 tagged release(s) since v1.25.0 … 26 ticket(s) carry their tag; 0 of 0 PR(s) merged since v1.28.0 carry release:v1.29.0; due 2026-09-12T16:07:14Z, 47h left` (docs/audits/logs/PMAT-227-gate-T-main.log); on the branch, red on the bypassed-review arm alone (docs/audits/logs/PMAT-227-gate-T-branch.log). `scripts/dogfood/release-check.sh` is the post-tag audit and its verdict is in docs/audits/release-1.28.0-receipt.md.
- No test was run and none was reverted: this branch contains no code. That is stated as `falsification.not_applicable` in the artifact and printed by the gate as NOT VERIFIED, which is the shape PMAT-224 built and PMAT-226 widened to admit the ledger.
- `pmat hooks install --strict`: every commit carries `Pmat-Ticket: PMAT-227`; the pre-commit hook accepted each one (no source file staged, so the complexity arm reports skipped by its own rule).
- I-3 `transcript-gate.sh` (the session directory): `PASS transcript-gate: attempted=14 denied=0 running_peak=1 slots=3 segments=429 files=14 (agent_calls=14 resumes=0 workflow_started=0; denied from hook log) (session <this session>, rule=explicit)`.
