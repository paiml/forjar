# Quorum evidence — PMAT-226 — pmat and the gates

- Roadmap entry PMAT-226 appended with `pmat work add … -t kind:code,orch:fable`, then `kind: code`, five acceptance criteria and `orch-basis:release` patched inside the one row textually; label `release:v1.28.0` applied at mint time with `scripts/release-goal.sh tag PMAT-226 v1.28.0` (the open window's tag).
- `kind-gate.sh`: kind=code files=0. `model-gate.sh`: refused once (the fresh row had `orch:fable` and no orch-basis token), then `model=fable class=fable decision=admit basis=file` (tier 1 [A] meets tier 1 [A]). `target-guard.sh`: PASS. `config-lint.sh`: slots=3 gh_calls_per_min=30 bank=3. `discover.sh`: gate_cmd=`cargo test --workspace`, gate_cmd_fallback=true (said here as the skill requires), quorum_tool=agy, code_search=pmat query.
- `goal.sh set` refused under R-5 (one ticket per session; PMAT-223 was set here) — the fourth ticket of this session, proceeding on the user's verbatim instruction quoted in the roadmap row's notes; `goal.sh worker` declared both lanes (ph3.crux width 1 teamwork, ph4.quorum width 3 quorum). `estimate.sh`: K̂=5 basis=first-run[U].
- Routing (`route.sh`, copied verbatim): research `route=agy-grillme w=1.00 basis=absent effort=1[U]` (the CRUX lane ran through the delegate as a teamwork lane — the nearest agy surface); review `route=agy-quorum w=1.00 basis=absent effort=1[U]` (followed); impl `route=agy-goal w=1.00 basis=absent note=fable-binding effort=1[U]` — the rail change, the bump and the CHANGELOG were executed by self (the deviation is named here: a one-line predicate plus prose, smaller than a lane brief); orchestration `route=self w=100.00 basis=absent`.
- `pmat hooks install --strict --force`: every commit carries `Pmat-Ticket: PMAT-226`; the pre-commit hook accepted every commit (format, complexity, clippy, SATD; TDG baseline refreshed by the hook).
- Gates on 568dc169: see docs/audits/dogfood-1.28.0-receipt.md for the nine `GATE` lines of one `make dogfood-release` run; individually before it: A `9 of 9 merged PR(s) since v1.27.0 carry a harness receipt`, E `9 of 9 … carry a quorum receipt`, T `cut in flight: Cargo.toml is at 1.28.0`, H `9 of 9`, B/C/D/G PASS. `cargo metadata --locked --offline` exit 0. bashrs on scripts/quorum-gate.sh: 0 errors (12 pre-existing warnings, unchanged).
- `pmat analyze vacuous-tests --path tests --format json` on 568dc169: 43 of 4713 tests examined flagged tree-wide (331 files parsed); 0 in the one touched test file, tests/falsification_quorum_gate_has_a_triage_shape.rs — nothing to accept.
- Not measured on this host: gate F's mutation arm (`cargo mutants`, PMAT-216) — on this branch the arm has nothing to mutate (no src/ change) and passes by coverage.sh's own tests-only rule; that is a measured zero, not a skip, and PMAT-216 stays open for branches that touch src/.
- I-3 `transcript-gate.sh` (the session directory): `PASS transcript-gate: attempted=13 denied=0 running_peak=1 slots=3 segments=392 files=13 (agent_calls=13 resumes=0 workflow_started=0; denied from hook log) (session <this session>, rule=explicit)`.
