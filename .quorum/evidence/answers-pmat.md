# Quorum evidence — PMAT-223 — pmat and the gates

- Roadmap entry PMAT-223 appended textually, `kind:code`, `orch:fable` with `orch-basis:M>=3` (four owning modules measured before the ticket was opened: core/task/probe.rs, core/executor/resource_ops.rs, core/executor/resource_meta.rs with machine_b.rs, transport/mod.rs), and the by-hand measurement of the dead cache recorded in its notes.
- `kind-gate.sh`: kind=code. `model-gate.sh`: model=fable class=fable decision=admit basis=file, tier 1 meets tier 1. `target-guard.sh`: PASS. `config-lint.sh`: slots=3.
- `pmat hooks install --strict --force`: every commit carries `Pmat-Ticket: PMAT-223`; the pre-commit hook ran on each commit; none was refused.
- Gate: `cargo test --workspace` (gate_cmd, `gate_cmd_fallback=true`) first ran RED at the lib binary — `cli::tests_build_image::cmd_build_with_load_flag_no_runtime`, a host-dependent test outside this diff (docker on PATH, `docker load` failing), filed as forjar#503 — then GREEN with `--no-fail-fast`: 316 binaries, 19,618 passed, 0 failed, 58 ignored, the same test passing on the rerun; clippy `--all-targets -D warnings` exit 0; rustfmt stable check exit 0; gate G `scripts/dogfood/contracts.sh` PASS (40 contracts, citations resolve). Reduced logs under docs/audits/logs/PMAT-223-*.log carry each full log's byte count and sha256.
- Not measured on this host: gate F's mutation arm (`cargo mutants`, PMAT-216); no mutation-score figure is claimed. Six discriminating mutations were run by hand (M1 reader guard, M2 reader base, M3 writer guard, N1 settle call, N2 outputs question, N3 force bypass); eight reduced logs show the named test FAILED under each and the clean tree rebuilt after.
- The pre-fix RED: the base case of the binary falsifier FAILED on 643363b3 at the `1 unchanged` assertion (the task ran again); the remote case passed there by accident, the cache being dead, and is red only against M1 — said in the test's module doc and in the receipt.
- I-3 `transcript-gate.sh`: PASS attempted=5 denied=0 running_peak=1 slots=3 (five delegate dispatches, no worker subagent, no resume, no Workflow)
