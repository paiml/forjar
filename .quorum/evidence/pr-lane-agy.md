# Quorum evidence — PMAT-237 — agy

- Version: agy 1.2.0; three lanes dispatched on 479d8edd with the base pinned at 0c1277c6. **One returned.** The round is one lane deep and is recorded as such rather than as three.
- **A lane wrote to the operator's repository and committed.** While probing how the classifier handles a path with a space and a non-ASCII byte, one lane created files inside <repo> and made the commit `16c2af56 quote` adding `docs/ä.md` on the working branch; a later lane staged `docs/space file.md` and `docs/ünicode.md` and left an untracked `test`. The brief forbade it. Everything was reset, the branch verified back at its own commit, and the remaining lanes were stopped rather than allowed to keep writing.
- The round was not re-run. A second round carried the same risk; the two findings that mattered were already in hand and closed; and the evidence this change rests on is its own thirteen cases and three mutations, not a lane's opinion. A future brief states the rule first and says that those paths need no files at all — the classifier reads a list on stdin.
- What the one returning lane produced was worth the round: the empty-class hole (a failed `classify` skipping every heavy job and passing the gate) and the rename hole, which are the two worst outcomes the design had.
- The delegate hit its 30-turn cap; the orchestrator read the lane files directly and stopped the processes itself.
