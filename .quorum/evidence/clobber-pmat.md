# Quorum evidence — PMAT-230 — pmat and the gates

- Roadmap entry PMAT-230 minted with `pmat work add … -t kind:code,orch:fable`, then `kind: code`, four acceptance criteria (the fourth added when round 3's finding widened the ticket) and `orch-basis:release` patched textually; label `release:v1.29.0` applied at mint time with `scripts/release-goal.sh tag`.
- `kind-gate.sh`: kind=code files=0. `model-gate.sh`: `model=opus class=opus decision=admit basis=file`. `target-guard.sh`: PASS. `goal.sh set`: refused under R-5 (sixth ticket of this session, naming PMAT-223); `goal.sh worker` declared each of the three review rounds (ph2.quorum, ph2.quorum2, ph2.quorum3).
- Routing (`route.sh`, copied verbatim): impl `route=agy-goal w=1.00 basis=absent note=fable-binding effort=1[U] bucket_collision=true` — executed by self, the deviation named: workers and lanes may not edit `.github/workflows`; review `route=agy-quorum w=1.00 basis=absent effort=1[U]` (followed, three rounds); orchestration `route=self w=100.00 basis=absent`.
- Gates: `cargo test --test falsification_release_workflow_shape` 10 passed at HEAD, and rule 9 alone red against main's workflows (docs/audits/logs/PMAT-230-gate-tests.log). Twelve mutation cases (docs/audits/logs/PMAT-230-rule9-mutations.log). `actionlint` over every workflow: the finding SET is identical to main's — 22 findings both sides, differing only in two in-script line numbers that moved because comments were added; actionlint has never exited 0 on this repository (the `clean-room` runner label is unknown to it), so the measurement is the diff of the finding sets, not the exit code. Both edited workflows still parse as YAML. `scripts/dogfood/tagged.sh`: GATE T PASS on the branch.
- `pmat analyze vacuous-tests --path tests --format json`: 43 of 4713 tests examined flagged tree-wide; 0 in the touched test file. Nothing to accept.
- Not measured: cargo mutants (PMAT-216), and on this branch there is nothing under src/ for it to mutate — gate F's mutation arm passes by coverage.sh's own tests-only rule, a measured zero.
- Line stops in docs/audits/jidoka.jsonl: the pre-commit complexity refusal whose chained red-proof checkout destroyed a staged fix; round 1's three findings; round 2's four.
- I-3 `transcript-gate.sh` (the session directory): `PASS transcript-gate: attempted=17 denied=0 running_peak=1 slots=3 segments=565 files=17 (agent_calls=17 resumes=0 workflow_started=0; denied from hook log) (session <this session>, rule=explicit)`.
