# Quorum evidence — PMAT-224 — pmat and the gates

- Roadmap entry PMAT-224 appended textually, `kind:code`, `orch:fable` with `orch-basis:M>=3` (four owning modules measured before the ticket was opened: scripts/quorum-gate.sh, scripts/quorum_evidence.py, the two gate fixture harnesses under tests/, and the paiml-implement skill's kind-gate.sh in another repository).
- `kind-gate.sh`: kind=code. `model-gate.sh`: model=fable class=fable decision=admit basis=file. `target-guard.sh`: PASS. `config-lint.sh`: slots=3. `goal.sh set` REFUSED (exit 2: one ticket per session, PMAT-223 was set here); the ticket proceeded on the operator's verbatim `continue autonomously using pmat-implement`, recorded in the receipt; the lanes were declared with `goal.sh worker` as usual.
- `pmat hooks install --strict --force`: every commit carries `Pmat-Ticket: PMAT-224`. The pre-commit hook REFUSED the first fix commit on cognitive complexity already on main (`check_manifest` 90, `check_claims` 83, limit 25; `main` 27 after the kind check); seven flat helpers were extracted, behaviour-preserving apart from the new kind guard, and the hook accepted the file; no `--no-verify` (jidoka row in docs/audits/jidoka.jsonl).
- Gate: `cargo test --workspace --no-fail-fast` (gate_cmd, `gate_cmd_fallback=true`) exit 0 on 15418d23 — 317 binaries, 19,624 passed, 0 failed, 58 ignored; clippy `--all-targets -D warnings` exit 0; rustfmt stable check exit 0; gate G `scripts/dogfood/contracts.sh` PASS (40 contracts validate, citations resolve); `bashrs lint scripts/quorum-gate.sh` 0 errors, 15 warnings, unchanged from main. Reduced logs under docs/audits/logs/PMAT-224-*.log carry each full log's byte count and sha256.
- Not measured on this host: gate F's mutation arm (`cargo mutants`, PMAT-216). Three discriminating mutations were run by hand (the citation shape never widens; the triage arm removed; the rail check emptied); each reduced log shows its named case FAILED and the clean tree re-ran green.
- The pre-fix RED: three of six fixture cases FAILED on main for the right reasons (`only 0/4`; `falsification is missing 'test'` twice) and the three controls passed there and after.
- I-3 `transcript-gate.sh`: PASS attempted=7 denied=0 running_peak=1 slots=3 (five delegate dispatches on PMAT-223 and two on PMAT-224 in this session; no worker subagent, no resume, no Workflow)
