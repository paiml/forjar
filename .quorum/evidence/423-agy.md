# Independent review — agy /teamwork (plan mode) — #423

Verdict as delivered: "a substandard, brittle vendoring strategy that
manually alters upstream Cargo.toml names, strips targets, and breaks
standard Cargo tooling". Six charges; each checked against the code.

TAKEN (changed the branch):

> "`.github/workflows/ci.yml:133` tests `--no-default-features` … the CI
> workflow is currently broken for `--no-default-features` because the macro
> `provable_contracts_macros::contract` is unconditionally imported."

Correct and the sharpest finding: the first cut's `contracts` feature made
the macro crate optional, and six modules import it unconditionally. The
deps are plain and required again; the CI job's command passes.

> "`Cargo.toml:130` uses a loose version constraint `"0.31.2"` instead of
> pinned `"=0.31.2"`."

Taken: the in-tree crate IS that version.

> "the macros crate lacks a `VENDORED.md` entirely … stripped its `[[test]]`
> directives, and deleted its `tests/` directory without any declaration."

Taken: its own VENDORED.md declares the unchanged `src/` and the trimmed
`tests/`.

> "`.github/workflows/binary-release.yml:129` incorrectly retains the
> obsolete comment claiming `aprender-contracts resolves from crates.io`."

Taken.

REFUTED (did not survive the code):

> "the vendored crate directly pulls standard dependencies like `regex` and
> `serde` from crates.io."

Ordinary third-party dependencies are not contract code and not the fetch
that failed CI.

> "the `unsafe_code = "allow"` exception … suppresses the workspace's
> stricter lint policy against existing upstream unsafe code."

That line is upstream's own, in the registry copy's manifest.

> CRUX: "manually rewriting the manifest package names … places it
> significantly below the industry default" (vs `cargo vendor`, `[patch]`).

Disagreed on the premise: the instruction was to own the crate, which is the
organisation's own monorepo shape (`aprender-compute` / lib `trueno`);
`[patch]` keeps it external by name and cannot publish it.

INCIDENT. This lane was launched with `--dangerously-skip-permissions`, and
while "checking" the release shape its prompt named, it executed
`cargo publish --workspace` and published `forjar-contracts-macros 0.31.2`
and `forjar-contracts 0.31.2` to crates.io at 19:42 UTC — ahead of stage S5.
Recorded in .pmat/jidoka.jsonl; review lanes now run sandboxed and without
permission skipping. The crates are kept [A]: they are the byte-faithful
copies the release needs at exactly that version.
