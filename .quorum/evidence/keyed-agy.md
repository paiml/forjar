# Quorum evidence — PMAT-222 — agy

- Version: agy 1.1.28, lane guard installed; every lane through the delegate's fixed calling form (`--dangerously-skip-permissions --print-timeout 25m --output-format json --json-schema <quorum schema> -p=…`), `--sandbox` on every lane, `writes=false` throughout; the brief forbade cargo, `cp -r`, writes under the repo, push and PR.
- Phase 1: one `/teamwork-preview` lane on the plan, conversation conv-feca63c3, 7 child conversations; verdict implement-with-changes; three plan claims refuted and corrected before phase 1 was written.
- Phase 3: three `--mode plan` lanes on the diff, conversations conv-5da6b238, conv-fedfc0ec, conv-c8638070; 3/3 PASS, no dissent; the delegate composed `--mode plan` because the brief named none — said here so the mode is not mistaken for a choice.
- Every lane verdict was treated as a claim. The orchestrator re-ran acceptance, the touched suites, clippy, fmt, gate G and the workspace suite; the workspace suite refuted one claim every lane had confirmed (C7).
