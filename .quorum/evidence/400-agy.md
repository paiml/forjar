# agy /teamwork lane — #400 / #401 / #386 (sandboxed plan-mode review, scrubbed HOME)

# VERDICT
The fixes correctly implement all constraints. The quorum gate resolves the target branch, the diff, the receipt, and the falsification test from the pushed SHA, completely decoupling the push validation from the local checkout state. The gitignore invariant is solid and defended against vacuous truths. The coverage caching checks accurately identify both `actions/cache` and `Swatinem/rust-cache`, with comprehensive poka-yoke tests ensuring they cannot quietly fail open.

# REFUTED CLAIMS
None. I could not break the new rules. 
- The gate no longer reads the receipt from the working tree (`scripts/quorum-gate.sh:414` reads via `git cat-file`).
- No script or workflow reads `.pmat/baseline.json` from the old place (the CB-200 ceiling safely moved to `scripts/ratchets/cb200-baseline.json`).
- The `git check-ignore --no-index` invariant cannot pass vacuously because it asserts the index is non-empty (`tests/falsification_no_tracked_file_is_gitignored.rs`).
- The coverage guard accurately detects `Swatinem/rust-cache` and the debug-info check binds strictly to the coverage job.

# CONFIRMED CLAIMS
1. **The gate resolves from the pushed sha:** The gate correctly binds the push target (`pushed="${local_sha:-HEAD}"`). The receipt is read out of the object database at the pushed SHA, making an uncommitted or untracked bypass impossible. Branch deletions (all-zero sha) correctly short-circuit before causing git failures. The `cargo test` cross-branch skip is explicitly permitted locally, but safely closed in CI because `quorum.yml` explicitly checks out the PR head (`.github/workflows/quorum.yml:43`).
2. **Tracked-and-ignored invariant and `.pmat/` hygiene:** The TDG pre-commit hook still works on a fresh clone because `.pmat` is an explicitly derived directory; `pmat` generates it on demand. `tests/falsification_no_tracked_file_is_gitignored.rs` actively feeds `git ls-files` output into `git check-ignore --no-index` and forces a failure if any paths overlap.
3. **Coverage guard is airtight:** The guard natively understands both `actions/cache` and implicit `Swatinem/rust-cache` (`scan.rs:148`). The debug-info check parses YAML structure to mandate `line-tables-only` optimization without false positives, ensuring the 70 GiB full DWARF tree never crashes runners. Self-hosted exemptions safely and correctly ignore `[self-hosted]` or `group:` runs because the project controls those disks.
4. **Falsifiers cannot pass vacuously:** Poka-yoke controls (`tests/falsification_hosted_jobs_do_not_cache_target/controls.rs`) drive synthetic, hardcoded workflows through the scanner to guarantee that a broken workflow is rejected and a fixed one is accepted. 

# UNIQUE FINDINGS
- The `--no-index` argument in the gitignore test is structurally load-bearing; without it, `git check-ignore` would quietly report tracked files as un-ignored, neutralizing the check entirely and hiding the defect.
- The quorum gate executes a clean environment reset (`test_env = {k: v for k, v in os.environ.items() if not k.startswith("GIT_")}`) for tests. This is a brilliant defense against the test runner accidentally operating against the global GIT_DIR exported by the hook environment (which was shown to maliciously rewrite the tree).

# CRUX SURVEY
Industry standard pre-push and CI hygiene management in massive ecosystems:
- **Kubernetes:** Relies heavily on **Prow** for pre-submit validation on pull requests rather than local pre-push hooks. Prow handles the context of the PR head remotely. The local push hook approach here that explicitly evaluates the pushed SHA via `cat-file` is significantly more rigorous than Kubernetes' local developer experience.
- **Terraform:** Uses GitHub Actions and the **pre-commit** framework. `pre-commit` typically only analyzes files changed in the working tree or staging area, not strictly what is in the pushed commit. The quorum gate's design of extracting the exact tree from the pushed ref is far above the industry default for local hooks.
- **Ansible:** Utilizes **tox** and **ansible-test** for CI. Cache exhaustion is typically handled reactively by clearing caches or provisioning larger self-hosted runners, not proactively blocked by workflow-parsing structural tests. The structural test for DWARF reduction and `rust-cache` eviction is a highly advanced, defensively-engineered CI guard well above the industry default.

## Disposition

No charge survived: the lane tried the working-tree reads, the branch-deletion path, the cross-branch skip (and confirmed quorum.yml checks out the PR head), the old baseline paths, the `--no-index` invariant and the cache/debug-info guard, and refuted each attack with file:line evidence. Verdict accepted; nothing to change.
