# Crux lane — PMAT-208 — how release automations consume their own checksums

Successfully created the Implementation Plan artifact detailing how release automations sequence draft creation, upload, and publication, and recommended the cargo-dist [X] posture for forjar.

## What forjar took from it

Every surveyed tool computes checksums from local files before touching the releases API; none reads a draft it has not published. forjar reads the release because the release is its staging area, which is the coupling the teamwork lane named. The immediate repair keeps the coupling and fixes its broken edge; the aggregation posture the panel recommends is the 1.27 candidate. Every third-party figure is documentation memory, marked [X].
