# agy /teamwork lane — #364 (sandboxed plan-mode review, scrubbed HOME)

Here is the full adversarial review report for the `fix/yanked-spin-pin` branch.

### VERDICT
The branch successfully and correctly implements a standing gate for yanked crates, the offline falsifier assertions are architecturally sound and non-vacuous, and the design rigorously exceeds standard ecosystem practices. 

### REFUTED CLAIMS
1. **Hypothesis that spin 0.9.9 is yanked, incompatible, or other yanks exist:** Refuted — `spin 0.9.9` is actively unyanked on crates.io, natively satisfies `wasmi`'s `^0.9` constraint, and the offline test's whole-lockfile sweep proved no other yanked crates existed at authoring time (`tests/falsification_lockfile_carries_no_known_yanked_pin.rs:121`).
2. **Hypothesis that the `--deny yanked` flag is forgiven:** Refuted — The `cargo audit` step executes synchronously without a `|| true` fallback or a `continue-on-error` job property, guaranteeing that a non-zero exit will red the lane (`.github/workflows/audit.yml:54`).
3. **Hypothesis that widening `deny.toml` to `all-features = true` would have been a better fix:** Refuted — The file states it is deployed centrally from `paiml/infra` and must not be edited locally, making a localized lockfile audit the only architecturally valid repository-level fix (`deny.toml:2`).
4. **Hypothesis that the known-yanked list test is a tautology:** Refuted — The test dynamically parses the live `Cargo.lock` text into memory and intersects it against the known-bad list, strictly evaluating the external artifact (`tests/falsification_lockfile_carries_no_known_yanked_pin.rs:200-231`).
5. **Hypothesis that the falsifiers can pass vacuously:** Refuted — Hardcoded anti-vacuity assertions will crash the test if the lockfile parser yields fewer than 100 packages (`tests/falsification_lockfile_carries_no_known_yanked_pin.rs:206`) or if exactly 1 `cargo audit` invocation isn't found in the workflow (`tests/falsification_lockfile_carries_no_known_yanked_pin.rs:261`).

### CONFIRMED CLAIMS
- The assertion that `cargo deny` was blind to `spin` because of `all-features = false` is correct; `wasmi` is an optional dependency for the WASM plugin feature, which `cargo deny` ignores by default unless explicitly told to resolve the full feature graph.

### UNIQUE FINDINGS
- Even if `all-features = true` had been set in `deny.toml`, the workflow would *still* have passed without alerting. `cargo-deny`'s default behavior for `yanked` is `warn`, requiring an explicit `yanked = "deny"` under `[advisories]` to actually fail the lane.

### CRUX SURVEY
- **Tokio (`tokio-rs/tokio`)**: Runs `cargo-deny` with `all-features = true` configured in its `deny.toml`, but lacks an explicit `yanked = "deny"` rule under advisories, allowing yanked crates to generate warnings without halting CI.
- **Rust-Analyzer (`rust-lang/rust-analyzer`)**: Does not aggressively run `cargo-deny` or `cargo audit` in its core CI matrix, relying predominantly on `npm audit` for the web and VSCode extension layers.
- **Cargo (`rust-lang/cargo`)**: Explicitly sets `yanked = "warn"` in its `deny.toml` and applies `continue-on-error: true` for its `cargo-deny` advisories workflow step, aggressively preventing yanked crates from breaking CI.
- **Conclusion**: This project's design is definitively **above** the industry default. The Rust ecosystem heavily favors emitting warnings and allowing CI pipelines to proceed, prioritizing workflow stability over halting on upstream yank signals.

## Disposition

No charge survived: spin 0.9.9 is un-yanked and satisfies wasmi's constraint; no other yanked pin is present; --deny yanked sits on a step whose failure reds the lane; deny.toml is deployed centrally and is not the place for the fix; the falsifier reads the live lockfile and refuses to pass on fewer than 100 packages or a workflow without exactly one cargo-audit invocation. Its unique finding (cargo-deny defaults yanked to warn even with all-features = true) is recorded in the crux lane. Verdict accepted; nothing to change. The lane ran after the gate refused a NotRun receipt — recorded as a correction to the first receipt, not as a deviation.
