# Quorum evidence — PMAT-223 — agy

- Version: agy 1.1.28 for the grill, 1.2.0 for the diff rounds; lane guard installed; every lane through the delegate's fixed calling form (`--dangerously-skip-permissions --print-timeout 25m --output-format json --json-schema <quorum schema> -p=…`), `--sandbox` on every lane, `writes=false` throughout; the brief forbade cargo, `cp -r`, writes under the repo, push and PR; each lane read its own `--shared` clone.
- Phase 1: one `/teamwork-preview` lane on the plan, conversation conv-7f56f8d3; fan-out unmeasurable (children unknown), so it counts as one reading; verdict implement-with-changes; two plan claims refuted and corrected before the fix was written.
- Phase 3, round 1 on dda3cf0e: conv-3b2d2bec, conv-516a4444, conv-b78cd7d5 — 1 PASS / 2 FAIL, every code claim confirmed, refusals on scope and on the count of mutation logs.
- Phase 3, round 2 on f6cb029a: conv-9c006b59, conv-9870b88c, conv-68351fc7 — 3 FAIL, every code claim confirmed, refusals on three doc sentences the diff had made false and on the test doc's wording; the delegate hit its turn cap after the lanes finished and the lane files were read directly.
- Phase 3, round 3 on 25b17924: conv-46a2f668, conv-611d36ba, conv-e73116e2 — 3 FAIL, every code claim confirmed; the majority refused on two more sentences (a comment at the cache-hit call site, one book line), the minority on three points the delegate's own cross-checks contradicted.
- Phase 3, round 4 on the final tree: conv-1cea7f91, conv-037ef0e1, conv-5df448d2 — 2 PASS / 1 FAIL; the FAIL refutes two sentences the text at that commit already corrects (a lane error); the delegate hit its turn cap before reducing and the orchestrator reduced.
- Every lane verdict was treated as a claim. The orchestrator re-ran acceptance, the touched suites, the seven mutation checks, clippy, fmt, gate G and the workspace suite; one lane prediction (P1, the settle) was first refuted by a hand run against a stale binary and then confirmed against the right one — the orchestrator's error, recorded in docs/audits/jidoka.jsonl.
