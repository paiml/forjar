# Quorum evidence — PMAT-226 — agy

- Version: 1.2.0; lane guard installed; every lane through the delegate's fixed calling form (`--dangerously-skip-permissions --print-timeout 25m --output-format json --json-schema <quorum schema>`, `--sandbox` on every review lane, writes=false).
- Phase 3 (CRUX survey): one `--mode goal`-shaped teamwork lane on the nine [1.28.0] paragraphs at 97e72ce6, conversation conv-ba0f8bcf; verdict PASS, nine rows; three of its forjar descriptions were corrected by the orchestrator after re-resolving every citation (see release-1.28.0-crux.md).
- Phase 4 (review): three `--mode plan` lanes on the diff at 568dc169 with the base pinned at 191770fa, conversations conv-8a855795, conv-9f491252, conv-8a38ece1; 1/3 PASS, 2/3 FAIL; lane-reduce not agreed (dissent 2). Both refutations are corrections to the claim text (C5's count, C11's wording) and neither touches the diff, so no second round was run on an unchanged diff; the corrected statements are in release-1.28.0-judges.md.
- Both delegate dispatches (`paiml-agy-delegate`, opus) hit their 30-turn cap AFTER every lane had written its file; the orchestrator read `<out_dir>/lane-*.json` directly and ran lane-reduce.sh itself, the same recovery PMAT-222..225 recorded.
- Every lane verdict was treated as a claim. The orchestrator re-ran the triage-shape test binary at HEAD and with main's gate script stashed, gate H, gates A/E/T, `cargo metadata --locked`, the merged-PR census, and `release-goal.sh sync --check`; `make dogfood-release` ran once on the committed tree (docs/audits/dogfood-1.28.0-receipt.md).
- Residue: `git status --porcelain` of the repository showed no tracked change before or after either dispatch (the stray untracked files predate this session); no lane copied a `target/`; disk 174G → 142G free across the session, all of it the shared cargo target and the coverage target.
