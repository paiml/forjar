# Quorum evidence — PMAT-224 — agy

- Version: agy 1.2.0; lane guard installed; every lane through the delegate's fixed calling form (`--dangerously-skip-permissions --print-timeout 25m --output-format json --json-schema <quorum schema> -p=…`), `--sandbox` on every lane, `writes=false` throughout; the brief forbade cargo, writes under the repo, push and PR; each lane read its own `--shared` clone.
- Phase 1: one `/teamwork-preview` lane on the plan, conversation conv-048ea2e6; fan-out unmeasurable, so it counts as one reading; two plan claims refuted and corrected before the fix was written.
- Phase 3: three `--mode plan` lanes on the diff, conversations conv-e96ec28b, conv-0bf1d1c3, conv-fe39eeb3; 3/3 PASS, lane-reduce agreed; one honest refutation (C5's wording) and one label artifact.
- Every lane verdict was treated as a claim. The orchestrator re-ran the six fixture cases, the two sibling gate binaries, the three mutations, `bashrs lint` on both revisions, clippy, fmt, gate G and the workspace suite.
