# Quorum evidence — PMAT-226 — CRUX

- One agy teamwork lane (conv-ba0f8bcf) surveyed the roster in scripts/dogfood/crux-reconcile.sh against the nine [1.28.0] paragraphs from documentation memory only; it returned nine rows, each naming three systems, with every [V] citation pointing at a line it had opened in its clone at 97e72ce6.
- The orchestrator re-resolved every citation against HEAD (9 of 9 in range) and re-pinned four to the exact line the claim needs (`src/core/planner/unprobed.rs:37` for the census function rather than :24, `src/tripwire/drift/task_check.rs:155-166` for `skip_reason` rather than :105, and both scripts/dogfood/tagged.sh arms for row 1). Three rows were corrected where the lane's description of forjar disagreed with the code it cited: the quorum receipt is hash-bound, not "cryptographically signed"; the #495 fix did not change `machine_is_local` (the three sites ask `probe_answers_for`/`controller_answers_for`); the rail is verified from the diff, not "cryptographically verified". The Method section of docs/audits/crux-1.28.0.md says so.
- Rows 1, 2 and 9 (release engineering) were re-cast onto roster systems — Kubernetes, Ansible, Chef, Terraform, SLSA, Nix — because the gate counts only names in its SYSTEMS list; the lane's Bazel and Docker claims for those rows were dropped as the ones least concretely described.
- Gate H on 568dc169: `GATE H PASS 9 of 9 behaviour bullet(s) under [1.28.0] reconciled in docs/audits/crux-1.28.0.md, each naming >= 3 of the 28 surveyed systems` (docs/audits/logs/PMAT-226-gate-H.log). All three review lanes re-ran it.
- Thirty-two system claims, all [X]; the limit section names the prow field names and Expeditor label spellings as the claims most likely to have moved.
