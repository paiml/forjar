# Quorum evidence — PMAT-225 — agy

- Version: agy 1.2.0; lane guard installed; every lane through the delegate's fixed calling form (`--dangerously-skip-permissions --print-timeout 25m --output-format json --json-schema <quorum schema> -p=…`), `--sandbox` on every lane, `writes=false` throughout; the brief forbade writes under the repository, `cp -r target`, push and PR; each lane read its own `--shared` clone; only lane 1 of a round could run cargo, into its own `CARGO_TARGET_DIR`.
- Phase 1 (plan grill): one `--mode plan` lane on the design before a line was written, conversation conv-c66cee49; verdict `do-not-implement-as-written` with seven findings; three changed the design (a stray id is never skipped for the next one; the release clock is the tag's creation instant, not the tagged commit's; the status tool reads the working tree), one was a real implementation hazard (the cadence arm on a non-overdue main), one was confirmed (a label survives `pmat work edit`), one was the free-rider at the cut (answered by `release-goal.sh cut` moving the label), one named the grep-under-pipefail printer.
- Phase 3 round 1: three `--mode plan` lanes on the diff at db298604, conversations conv-72471c83-adjacent set recorded in the lane files (three ids); 3/3 FAIL, lane-reduce not agreed; two material findings: the resolver's refusal of an alias declared by two rows was reachable and untested (all three lanes), and `release-goal.sh window` omitted the receipt keys so the ledger rows were not byte-identical to its output (lane 1, claim C7). Fixed at ca3f8d70.
- Phase 3 round 2: three `--mode plan` lanes on ca3f8d70, conversations conv-72471c83, conv-c906c98c, conv-3f64b007; 3/3 PASS, lane-reduce agreed (exit 0), zero dissent; F2 (byte identity) measured by two lanes and cited by one — the orchestrator's five diffs are the tiebreaker; H2's second half left open by all three: `cut` does not itself verify a receipt exists, gate T refuses an unbacked `dogfood:` key downstream (`not at HEAD`).
- Phase 4: one `--mode plan` lane for the CRUX survey (see goal-crux.md).
- Every lane verdict was treated as a claim. The orchestrator re-ran gate T and its mutation, the five `window` diffs, the six test binaries, bashrs on five scripts, actionlint, gates B and G, clippy and the workspace suite (docs/audits/logs/PMAT-225-*.log).
- Residue: `git status --porcelain` of the repository byte-identical before and after every round; disk 232G → 185G free across the session (one cold `CARGO_TARGET_DIR` per cargo lane, in scratch, never a copied `target/`).
